# Module 12 Completion Report

## Instruction File
- Filename: instructions/use-reportinputschema.agent.md

# Use Report Input Schema Tool

Use this instruction when the user needs to generate a valid weekly status report input payload from command-line arguments.

When to use the tool:
- The user wants to create or update a report JSON payload for `tasks`, `milestones`, `risks`, `dependencies`, or `next_week_priorities`.
- The user has already gathered the report data and wants a structured JSON schema output.
- The user wants to quickly turn command-line data into the expected report input format.

Tool to use:
- `tools/reportinputschema.py`

How to invoke it:
- Run:
  `python tools/reportinputschema.py --tasks '<json-array>' --milestones '<json-array>' [optional arguments]`
- Required arguments:
  - `--tasks`: JSON array of task objects
  - `--milestones`: JSON array of milestone objects
- Optional arguments:
  - `--risks`: JSON array of risks or strings
  - `--dependencies`: JSON array of dependency objects
  - `--next-week-priorities`: JSON array of priority strings
  - `--headline`: report headline
  - `--report-date`: report date
  - `--summary`: summary text

Example:
- `python tools/reportinputschema.py --tasks '[{"id":"TASK-101","title":"Finalize sprint planning","status":"Completed","owner":"Alex","priority":"High","due":"2026-09-08","note":"Planning completed."}]' --milestones '[{"id":"MS-01","name":"Migration phase 1","status":"On Track","target_date":"2026-09-30","completion_percent":65}]'`

How to present results:
- Return the JSON payload generated by the tool.
- Keep the response concise and focused on the created report schema.
- If the user asked for a file, save the JSON output to the requested location.
- If values are invalid, clearly explain the invalid JSON input and ask for corrected data.

## Script File
- Filename: tools/reportinputschema.py
- Language: Python

#!/usr/bin/env python3
"""Build a report input schema from command-line arguments.

This script accepts the core report fields needed by the weekly status report
input schema and emits a JSON object that can be saved to a file or piped into
another process.

Example:
    python tools/reportinputschema.py \
        --headline "Weekly Status Report" \
        --report-date "September 10, 2026" \
        --summary "Weekly summary" \
        --tasks '[{"id":"TASK-101","title":"Finalize sprint planning","status":"Completed","owner":"Alex","priority":"High","due":"2026-09-08","note":"Planning completed."}]' \
        --milestones '[{"id":"MS-01","name":"Migration phase 1","status":"On Track","target_date":"2026-09-30","completion_percent":65}]' \
        --risks '["Partner team response is pending."]' \
        --dependencies '[{"id":"DEP-01","description":"External API dependency","owner":"Priya","status":"Blocked","action_required":"Confirm partner response."}]' \
        --next-week-priorities '["Resolve dependency with partner team."]'
"""

import argparse
import json
import sys
from typing import Any


def parse_json_argument(value: str, field_name: str) -> Any:
    """Parse a CLI JSON argument for a field such as tasks or milestones."""
    try:
        return json.loads(value)
    except json.JSONDecodeError as exc:
        raise SystemExit(
            f"Invalid JSON for {field_name}: {value!r}. Error: {exc.msg}"
        ) from exc


def parse_optional_json_argument(value: str | None, field_name: str) -> Any:
    if value is None:
        return []
    return parse_json_argument(value, field_name)


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(
        description="Build a weekly report input schema JSON payload from command-line arguments."
    )

    parser.add_argument(
        "--headline",
        default="Weekly Status Report",
        help="Headline for the report (default: Weekly Status Report)",
    )
    parser.add_argument(
        "--report-date",
        default="",
        help="Report date string (optional)",
    )
    parser.add_argument(
        "--summary",
        default="",
        help="High-level summary text (optional)",
    )
    parser.add_argument(
        "--tasks",
        required=True,
        help='JSON array of tasks. Example: \'[{"id":"TASK-101", "title":"Task", "status":"Completed", "owner":"Alex", "priority":"High", "due":"2026-09-15", "note":"Notes"}]\'',
    )
    parser.add_argument(
        "--milestones",
        required=True,
        help='JSON array of milestones. Example: \'[{"id":"MS-01", "name":"Migration phase 1", "status":"On Track", "target_date":"2026-09-30", "completion_percent":65}]\'',
    )
    parser.add_argument(
        "--risks",
        help="JSON array of risks or strings. Optional.",
    )
    parser.add_argument(
        "--dependencies",
        help="JSON array of dependencies. Optional.",
    )
    parser.add_argument(
        "--next-week-priorities",
        help="JSON array of next week priorities. Optional.",
    )

    return parser


def main() -> None:
    parser = build_parser()
    args = parser.parse_args()

    report = {
        "headline": args.headline,
        "report_date": args.report_date,
        "summary": args.summary,
        "tasks": parse_json_argument(args.tasks, "tasks"),
        "milestones": parse_json_argument(args.milestones, "milestones"),
        "risks": parse_optional_json_argument(args.risks, "risks"),
        "dependencies": parse_optional_json_argument(args.dependencies, "dependencies"),
        "next_week_priorities": parse_optional_json_argument(
            args.next_week_priorities, "next_week_priorities"
        ),
    }

    # Ensure the required top-level structure is JSON-serializable.
    json.dump(report, sys.stdout, indent=2)
    sys.stdout.write("\n")


if __name__ == "__main__":
    main()

## Script Execution Output
usage: reportinputschema.py [-h] [--headline HEADLINE]
                            [--report-date REPORT_DATE] [--summary SUMMARY]
                            --tasks TASKS --milestones MILESTONES
                            [--risks RISKS] [--dependencies DEPENDENCIES]
                            [--next-week-priorities NEXT_WEEK_PRIORITIES]

Build a weekly report input schema JSON payload from command-line arguments.

options:
  -h, --help            show this help message and exit
  --headline HEADLINE   Headline for the report (default: Weekly Status
                        Report)
  --report-date REPORT_DATE
                        Report date string (optional)
  --summary SUMMARY     High-level summary text (optional)
  --tasks TASKS         JSON array of tasks. Example: '[{"id":"TASK-101",
                        "title":"Task", "status":"Completed", "owner":"Alex",
                        "priority":"High", "due":"2026-09-15",
                        "note":"Notes"}]'
  --milestones MILESTONES
                        JSON array of milestones. Example: '[{"id":"MS-01",
                        "name":"Migration phase 1", "status":"On Track",
                        "target_date":"2026-09-30", "completion_percent":65}]'
  --risks RISKS         JSON array of risks or strings. Optional.
  --dependencies DEPENDENCIES
                        JSON array of dependencies. Optional.
  --next-week-priorities NEXT_WEEK_PRIORITIES
                        JSON array of next week priorities. Optional.

---
usage: validatereportdata.py [-h] [--file FILE] [--data DATA]
                             {dependencies,milestones,next_week_priorities,risks
,tasks}

Validate a named report section from a JSON report payload.

positional arguments:
  {dependencies,milestones,next_week_priorities,risks,tasks}
                        Section name to validate: tasks, milestones, risks,
                        dependencies, or next_week_priorities

options:
  -h, --help            show this help message and exit
  --file, -f FILE       Path to a JSON report file. If omitted, the script
                        reads from stdin.
  --data, -d DATA       Inline JSON string to validate instead of reading from
                        a file or stdin.
