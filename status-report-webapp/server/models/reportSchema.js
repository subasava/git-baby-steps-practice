const reportSchema = {
  headline: 'string',
  report_date: 'string',
  summary: 'string',
  tasks: 'array',
  risks: 'array',
  next_week_priorities: 'array',
};

module.exports = {
  reportSchema,
};
